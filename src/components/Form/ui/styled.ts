import { styled, Box, Button } from "@mui/material";

export const BoxFormStyled = styled(Box)(() => ({
  display: "flex",
  justifyContent: "center",
  flexDirection: "column",
}));

export const ButtonSubmitStyled = styled(Button)(() => ({
  margin: "8px 0 8px 0",
}));
export const ButtonChangeModalStyled = styled(Button)(() => ({
  margin: "8px 0 8px 0",
}));
